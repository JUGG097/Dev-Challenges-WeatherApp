import styled from "styled-components";

export const StyledAirPressure = styled.div`
	background-color: #1e213a;
	padding: 10px 20px;

	p {
		font-size: 14px;
		font-weight: 500;
	}

	p:first-child {
		font-size: 16px;
	}

	.pressure-value {
		font-size: 60px;
		font-weight: 700;
	}

	.pressure-value span {
		font-size: 30px;
		font-weight: 500;
	}
`;
