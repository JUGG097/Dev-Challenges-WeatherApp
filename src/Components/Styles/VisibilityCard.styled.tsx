import styled from "styled-components";

export const StyledVisibilityCard = styled.div`
	background-color: #1e213a;
	padding: 10px 20px;

	p {
		font-size: 14px;
		font-weight: 500;
	}

	p:first-child {
		font-size: 16px;
	}

	.visibility-value {
		font-size: 60px;
		font-weight: 700;
	}

	.visibility-value span {
		font-size: 30px;
		font-weight: 500;
	}
`;
